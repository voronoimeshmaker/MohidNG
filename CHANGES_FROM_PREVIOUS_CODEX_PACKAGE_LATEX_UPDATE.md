# Additional update after the previous Codex package

This package includes all LaTeX source files generated during the Mohid-NG planning discussion.

The added files are stored in:

```text
planning/latex_documents/
```

These LaTeX documents are reference/planning material. Codex should not treat them as source code to compile as part of the C++ build. They are included so the project can preserve and copy the planning documents directly into the repository.

The current main architectural reference is:

```text
planning/latex_documents/mohid_ng_plano_arquitetural_v5.tex
```

Earlier versions are retained for traceability.
