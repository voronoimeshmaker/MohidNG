# Testing

Every new feature shall define its passage criteria before it is considered complete.

A module should include:

```text
unit tests
integration tests
parallel tests, if applicable
restart tests, if state persistence is affected
validation tests, if a numerical method is introduced
examples, if the feature is user-facing
```
